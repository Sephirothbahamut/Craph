#include "stdafx.h"
#include "act.h"

namespace mylang
	{
	/*namespace act
		{
		namespace var
			{
			root::root(type t) : t(t) {}
			number::number() : root(type::num) {}
			function::function(std::vector<stm::root*>* stms) : root(type::fun), stms(stms) {}
			}
		global_scope::global_scope(std::vector<stm::dec::root*>* stms) : stms(stms) {}
		}*/
	}